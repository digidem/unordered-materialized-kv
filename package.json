{
  "name": "unordered-materialized-kv",
  "version": "1.2.1",
  "description": "materialized view key/id store based on unordered log messages",
  "dependencies": {
    "once": "^1.4.0"
  },
  "devDependencies": {
    "memdb": "^1.3.1",
    "tape": "^4.9.0"
  },
  "scripts": {
    "test": "tape test/*.js"
  },
  "license": "BSD"
}
